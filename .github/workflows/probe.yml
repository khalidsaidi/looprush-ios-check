name: probe
on: { workflow_dispatch: {} }
jobs:
  ubuntu: { runs-on: ubuntu-latest, steps: [ { run: "echo ubuntu ok" } ] }
  macos:  { runs-on: macos-latest,  steps: [ { run: "sw_vers && xcrun simctl list devices available | grep -c iPhone" } ] }
