# looprush-ios-check

Mobile Safari (iOS Simulator) and desktop Safari checks for https://looprush2.com,
run on GitHub's free macOS runners for public repositories. The scripts drive the
live site only; there are no secrets here. Mirrors `scripts/ci/` in the app repo.
